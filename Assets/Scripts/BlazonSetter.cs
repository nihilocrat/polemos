using UnityEngine;
using System.Collections;

public class BlazonSetter : MonoBehaviour
{
	public int team = 0;
	
	// Use this for initialization
	void Start ()
	{
		/*
		var player = Player.GetPlayer(team);
		
		if(renderer != null)
		{
			renderer.sharedMaterial = player.GetBlazon();
		}
		
		foreach(Renderer render in GetComponentInChildren<Renderer>())
		{
			render.sharedMaterial = player.GetBlazon();
		}
		*/
	}
}
